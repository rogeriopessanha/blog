<%- include('partials/header') %>
    <%- include('partials/homenavbar') %>

        <body>
            <div class="container">
                <% artigo.forEach(artigo=> { %>
                    <div class="card">
                        <div class="card-header">
                            <h3>
                                <%= artigo.titulo %>
                            </h3>
                        </div>

                        <div class="card-body">
                            <a href=""><button class="btn btn-success">Ler artigo</button></a>
                        </div>
                    </div>
                    <br>
                    <% }) %>
            </div>

        </body>

        <%- include('partials/footer') %>